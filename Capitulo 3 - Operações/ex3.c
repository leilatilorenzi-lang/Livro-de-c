#include <stdio.h>

int main() {

    int x, y, z, total;
    x = 5;
    y = 3;
    z = 6;

    printf("x = %d\ny = %d\nz = %d\n", x, y, z);
    total = x + y + z;

    printf("o valor total é: %d ", total);
    return 0;
}